{
  "name": "genatrix-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "The Genatrix interface: one daemon, any device with a browser (design 06).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "@tsconfig/svelte": "^5.0.4",
    "svelte": "^5.20.0",
    "svelte-check": "^4.1.4",
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  },
  "dependencies": {
    "qrcode-generator": "^1.4.4"
  }
}
